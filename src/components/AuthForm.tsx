import { useState, useEffect } from 'react'; const [password, setPassword] = useState(''); useEffect(() => { const hashedPassword = hashPassword(password); // use a hashing library like bcrypt }, [password]);